Extract the code visible in this image. Adapt to context — this could be a full-screen IDE, a terminal window, a presentation slide with code snippets, a handwritten whiteboard, a printed page, a mobile screen. Preserve indentation, syntax, and structure exactly. Note the background (dark/light theme) and any visible syntax highlighting. If line numbers are present, omit them unless they are part of the code. If the code is incomplete or partially cut off, indicate where. Output in a code block with the detected language.
