Compare these two images. Identify what is different between them. Adapt to the image type — these could be UI screenshots, photos, documents, diagrams, charts, or anything else. For each difference: describe WHAT changed, WHERE (use natural position language, not pixel coordinates), and HOW SIGNIFICANT it is (major/minor). If the images are completely different subjects, state that first. If they are identical, just say so — don't invent differences. Organize findings from most to least significant.
